# non-browser tests

* Somehow importing SolidJS Store ("@solidjs/store") causes browser mode to emit error.
* Put here the tests that not use Vitest browser mode / DOM operations for clarification.
